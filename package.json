{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "./server/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-sass": "node-sass app/css/style.scss app/css/style.css",
    "watch-sass": "nodemon -e scss -w app/css -x 'npm run build-sass'",
    "watch-app-js": "nodemon -e js -w app/js",
    "watch-server-js": "nodemon -e js -w server/js",
    "watch": "npm run watch-sass && npm run watch-app-js && npm run watch-server-js && nodemon"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^2.0.0",
    "express": "^4.13.4",
    "http": "0.0.0",
    "serve-favicon": "^2.3.0"
  }
}
